# Tech

Default: React + Vite + TypeScript

Styling: Tailwind CSS v4 (Vite plugin)

UI Components: shadcn/ui (Radix-based, copied to `src/components/ui`)

State: (pick one) TanStack Query (data) + Zustand (UI) or "React-only"

Testing: Vitest + Testing Library

Lint/format: ESLint + Prettier

Path aliases: `@/` → `src/`