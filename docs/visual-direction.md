# Visual Direction

> **This file is auto-generated.** Run `/generate-visual-direction` in OpenCode after adding images to `docs/moodboard/` and `docs/screenshots/` to populate it.
>
> Once generated, the agent will read this file each session instead of re-reading the source images.

---

_No visual direction has been generated yet for this project._

Add your reference images to `docs/moodboard/` and/or `docs/screenshots/`, then run:

```
/generate-visual-direction
```
